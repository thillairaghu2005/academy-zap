# Server-only URL used by the same-origin Next.js API proxy.
ZAPSTERS_API_URL=http://127.0.0.1:8000

# Optional deferred-surface demo controls. Keep disabled for production.
NEXT_PUBLIC_DEMO_MODE=false

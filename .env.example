# Required for signed auth cookies. Use a different random value per deployment.
SESSION_SECRET=replace-with-a-long-random-secret

# Optional local-only demo auto-login. Keep false or unset on deployed environments.
NEXT_PUBLIC_DEMO_MODE=false
